package com.digitalbank.app;

import com.digitalbank.domain.Account;
import com.digitalbank.domain.Client;
import com.digitalbank.service.AccountService;
import com.digitalbank.service.TransactionService;

public class Main {
    public static void main(String[] args) {
        AccountService accountService = new AccountService();
        TransactionService transactionService = new TransactionService();

        Client alice = new Client("Alice");
        Client bob = new Client("Bob");

        Account acc1 = accountService.createAccount(new Account(alice) {});
        Account acc2 = accountService.createAccount(new Account(bob) {});

        acc1.deposit(1000);
        acc1.withdraw(200);

        transactionService.transfer(acc1, acc2, 300);

        acc1.printStatement();
        acc2.printStatement();
    }
}







































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































